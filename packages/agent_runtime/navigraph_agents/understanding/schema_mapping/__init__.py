"""Schema Mapping agent: assembles resolved concept/relationship/semantic
matches plus a catalog inventory into a concrete set of tables, columns,
and joins ready for SQL generation. Fully deterministic (pure function of
its input) -- see agent.py for the implementation."""
