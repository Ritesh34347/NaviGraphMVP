"""Metadata Discovery agent: reads the crawled catalog (tables, columns,
business glossary) for a data source and assembles a flat, glossary-enriched
column listing. Fully deterministic -- no LLM call -- see agent.py."""
