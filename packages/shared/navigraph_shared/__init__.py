"""NaviGraph shared foundation package.

Contains the contracts, telemetry, config, and LLM client abstractions that
every agent, the gateway, and the agent-runtime service build on top of.
"""

__version__ = "0.1.0"
