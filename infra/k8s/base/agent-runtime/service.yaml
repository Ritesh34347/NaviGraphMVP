apiVersion: v1
kind: Service
metadata:
  name: agent-runtime
  namespace: navigraph
  labels:
    app: agent-runtime
spec:
  selector:
    app: agent-runtime
  ports:
    - port: 8001
      targetPort: 8001
