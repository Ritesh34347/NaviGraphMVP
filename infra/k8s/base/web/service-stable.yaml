apiVersion: v1
kind: Service
metadata:
  name: web-stable
  namespace: navigraph
  labels:
    app: web
    track: stable
spec:
  selector:
    app: web
    track: stable
  ports:
    - port: 3000
      targetPort: 3000
