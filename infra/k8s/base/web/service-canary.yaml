apiVersion: v1
kind: Service
metadata:
  name: web-canary
  namespace: navigraph
  labels:
    app: web
    track: canary
spec:
  selector:
    app: web
    track: canary
  ports:
    - port: 3000
      targetPort: 3000
